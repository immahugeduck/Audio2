# GEMINI_API_KEY: Required for Gemini AI API calls (noise classification + transcription).
# Local dev: put this in a .env file (gitignored) - server.ts loads it automatically.
# Vercel: set this in Project Settings -> Environment Variables. Do NOT commit real keys.
GEMINI_API_KEY="your-gemini-api-key-here"
